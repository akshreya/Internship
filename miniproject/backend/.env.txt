PORT=4000

# Gmail SMTP — see setup steps in chat for getting an App Password
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=akshreya840@gmail.com
SMTP_PASS=pwgtglxamorouzen

# Where "new request" notifications get sent (can be your own email)
ADMIN_EMAIL=admin@example.com
